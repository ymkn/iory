# AGENTS.md

## この文書の役割
- この文書は「どのファイルがあるか」ではなく、「IORY でどう判断するか」を残すためのもの。
- ソースを読めば分かる構造説明より、変更時に迷いやすい設計思想・優先順位・地雷を記録する。
- 実装や UX の方向が変わったら、コードだけでなくこの文書と `spec/` も更新する。

## 製品の前提
- IORY は **単一ファイルを静かに書くためのデスクトップアプリ** である。
- 主役は常にユーザーのローカルファイルであり、アプリ独自のプロジェクト形式や閉じたデータモデルを中心にしない。
- フォルダ管理ツールや作品マネージャーではなく、「いま開いている 1 ファイルに集中する」ことを優先する。
- Windows を品質基準に置く。特に WebView2 / IME / native dialog / custom titlebar の自然さは最優先事項。

## 判断の優先順位
1. **本文を失わないこと**
2. **執筆中に意識を奪わないこと**
3. **ファイルの所在と状態がユーザーにとって明確であること**
4. **見た目の美しさや新機能は、その後に置くこと**

迷ったら、派手さや便利さよりも、保存安全性と静かな集中体験を優先する。

## UX / UI 方針

### ミニマル原則
- 説明文は極力減らす。ラベルで十分なら文章を書かない。
- デフォルトでは装飾的な border、border 付きカード、強調のための太字を使わない。
- 強調は box ではなく、余白、整列、控えめな色差、小さなタイポグラフィ階層で作る。
- スプラッシュ、空状態、サイドペイン、通知は同じ静かな言語で揃える。

### レイアウト原則
- 左ペインは「current → document → recent files」、右ペインは checkpoint timeline に固定する。
- ここに workspace tree 的な責務を戻さない。戻すなら、執筆体験上の明確な理由が必要。
- ラベル / 値 UI は **ラベルを上、値を次の行** に置く。値は自身の行の中だけで右揃えにする。
- 長い値がラベルと競合する 1 行レイアウトは避ける。

### 通知原則
- 上部メッセージはアラート箱ではなく、控えめなテキスト通知として扱う。
- 成功や情報通知は短時間で自然に消えるべき。
- error / conflict のように判断を要するものは消さずに残す。

## 保存・履歴の考え方

### ソースオブトゥルース
- 真実の本文は常にディスク上のファイルである。
- ローカル履歴は補助的な安全策であり、主データストアではない。

### 保存原則
- autosave は「ユーザーが保存を意識しなくて済む」ことが目的で、存在感を出しすぎない。
- 保存フローを変えるときは、Windows のアトミックセーブ意味論を壊さないこと。
- native dialog 中やファイル切替中は、保存・外部同期・checkpoint のタイミング競合に注意する。
- Windows / OneDrive 上では `ReplaceFileW` が数秒ブロックすることがある。保存処理は UI / IPC スレッドを巻き込まない形で実行する。
- `sync_all` より `ReplaceFileW` が支配的に遅いケースが確認されている。OneDrive 対策で保存方式を変える場合は、まず原子的置換の意味をどう維持するかを検討する。
- autosave と manual save で保存強度を分ける案はあり得るが、本文保護の優先順位を下げる変更なので、計測結果と復旧方針をセットで判断する。

### checkpoint 原則
- checkpoint は path 単位のローカルスナップショットとして扱う。
- 定期 checkpoint はデフォルト 5 分。**内容に変化がないなら追加しない**。
- 明示的保存（Ctrl+S）は「保存成功後」に明示 checkpoint を残す。ただし最新 snapshot と同一なら増やさない。
- restore は単なる閲覧ではなく、**ディスク書き戻し + それ以後のローカル履歴切り捨て** を伴う操作として扱う。
- restore UI は「巻き戻し」であることを曖昧にしない。

## 競合と外部変更
- ローカル未編集なら、外部変更は静かに再読込してよい。
- ローカル未保存変更があるなら、自動でマージや上書きをしない。必ず止まって選ばせる。
- conflict 時に重要なのは「自動解決の賢さ」ではなく、「何が起きたかを壊さず伝えること」。

## IME / Windows リスク方針
- エディタの再マウント、focus 制御、fullscreen、titlebar drag、overlay 表示はすべて IME 感度が高い。
- editor lifecycle を変える変更は軽く扱わず、Windows 実機前提で考える。
- ブラウザ上で自然でも、Tauri + WebView2 で自然とは限らない。

## 変更時の判断材料
- その変更は「書くこと」以外の認知負荷を増やしていないか。
- 状態説明のために、新しい箱・色・太字を増やしていないか。
- 保存・復元・競合の意味が曖昧になっていないか。
- 単一ファイルアプリという前提を崩していないか。
- 将来の LLM や補助機能の余地を残しつつ、現時点の UI を重くしていないか。

## デバッグ / 計測
- リリースビルドで常時ログを増やさない。必要な計測は `debug_assertions` または `IORY_DEBUG_LOG` で有効化する。
- 計測ログは「何を待っているか」を判断できる粒度にする。保存なら encode / temp write / flush / atomic replace / metadata のように分ける。
- ログには本文や絶対パスを出さない。必要ならファイル名、サイズ、文字数、経過時間に留める。
- 計測は原因を確定したら削るのではなく、将来の Windows / OneDrive / WebView2 回帰調査に使えるなら gated log として残してよい。

## ビルド / リリース
- バージョン表示にはビルドアーキテクチャも出す。ARM64 / x64 の取り違えは Windows on ARM 調査で重要な判断材料になる。
- ARM64 環境での再現確認は、x64 エミュレーションか ARM64 ネイティブかを必ず分けて見る。
- リリース前にローカルでリリースビルドを作る場合は、対象アーキテクチャを明示する。GitHub Actions のリリース生成条件とも整合させる。

## 今後も維持したい方向性
- 「静かで、少ない UI で、しかし本文保護だけは強い」こと。
- データ所有権はユーザー側、安心感はアプリ側、という役割分担を崩さないこと。
- 派手さよりも、自然さ・一貫性・壊れにくさを積み上げること。

## Push時のルール

Push前にバージョンを上げるかどうかユーザに聞くこと。バージョンを上げる場合はリリースビルドを作成すること。
